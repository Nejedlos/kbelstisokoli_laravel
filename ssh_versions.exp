#!/usr/bin/expect -f
set timeout 10
set password "oaFZeUjI"
spawn ssh -p 20001 -o StrictHostKeyChecking=no ssh-588875@dw191.webglobe.com "php8.4 -v; php -v; mysql --version"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
