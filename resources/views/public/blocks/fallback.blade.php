<section class="section-padding">
    <div class="container">
        <div class="p-6 bg-white border-2 border-dashed border-slate-200 rounded-club text-center text-slate-500">
            <p class="font-bold">{{ __('general.blocks.unknown_block') }}: <span class="text-primary">{{ $type ?? 'unknown' }}</span></p>
            <p class="mt-2 text-sm">{{ __('general.blocks.unknown_block_desc') }}</p>
        </div>
    </div>
</section>
